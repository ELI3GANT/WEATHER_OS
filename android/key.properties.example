# Android Keystore configuration template for WeatherOS (OnlyTruePerspective LLC)
# Copy this file to android/key.properties and populate with production release credentials:
#
# storePassword=<keystore password>
# keyPassword=<key password>
# keyAlias=<key alias>
# storeFile=<path/to/upload-keystore.jks>
